<!-- res/drawable/circle_button.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/number_buttons.pressed"/>
            <corners android:radius="40dp"/>
            <size
                android:width="@dimen/_100sdp"
                android:height="@dimen/_100sdp"/>
        </shape>
    </item>
    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/number_buttons"/>
            <corners android:radius="40dp"/>
            <size
                android:width="@dimen/_42sdp"
                android:height="@dimen/_42sdp"/>
        </shape>
    </item>
</selector>
